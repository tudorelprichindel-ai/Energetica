# The Power Tarot

Source application for the free tarot experience published at `/tarot/`.

The production build is written to the repository-level `tarot/` directory so GitHub Pages can serve it alongside the existing static website. Card artwork is shared from `assets/tarot/cards/` to avoid storing duplicate image sets.

The journal, daily card and onboarding preferences are stored only in the visitor's browser.
